# PyQt5 must be installed via apt on Ubuntu/Debian, NOT pip:
#   sudo apt install python3-pyqt5
# The pip wheel bundles its own Qt and breaks the xcb platform plugin.
pyperclip>=1.8.0
pynput>=1.7.0
Pillow>=9.0.0

# Dev / testing
pytest>=7.0.0
pytest-qt>=4.2.0
